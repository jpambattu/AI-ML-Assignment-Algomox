Run the model_level_c.py first.


Then run the app.py


Model is deployed using FLASK.


Go to the http://127.0.0.1:5000 URL. 5000 is the default port number of FLASK.

If the UI is not rendered properly, try reloading the page!


User input complaints and prediction for the same is saved in the 'custom_input.csv' file